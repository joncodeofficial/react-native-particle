///
/// JHybridParticleCanvasViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridParticleCanvasViewStateUpdater.hpp"
#include "views/HybridParticleCanvasViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::particle::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridParticleCanvasViewState>;

void JHybridParticleCanvasViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridParticleCanvasViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  std::shared_ptr<JHybridParticleCanvasViewSpec> hybridView = javaView->getJHybridParticleCanvasViewSpec();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridParticleCanvasViewState& data = concreteState->getData();
  const std::shared_ptr<HybridParticleCanvasViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    // Props aren't set yet!
    throw std::runtime_error("HybridParticleCanvasViewState's data doesn't contain any props!");
  }

  // Update all props if they are dirty
  if (props->preset.isDirty) {
    hybridView->setPreset(props->preset.value);
    props->preset.isDirty = false;
  }
  if (props->count.isDirty) {
    hybridView->setCount(props->count.value);
    props->count.isDirty = false;
  }
  if (props->emitterX.isDirty) {
    hybridView->setEmitterX(props->emitterX.value);
    props->emitterX.isDirty = false;
  }
  if (props->emitterY.isDirty) {
    hybridView->setEmitterY(props->emitterY.value);
    props->emitterY.isDirty = false;
  }
  if (props->loop.isDirty) {
    hybridView->setLoop(props->loop.value);
    props->loop.isDirty = false;
  }
  if (props->emitInterval.isDirty) {
    hybridView->setEmitInterval(props->emitInterval.value);
    props->emitInterval.isDirty = false;
  }

  // Update hybridRef if it changed
  if (props->hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props->hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
    props->hybridRef.isDirty = false;
  }
}

} // namespace margelo::nitro::particle::views
